/*
  Backend credientals for the mysql backend with the following information given:
    1. name = specifies the name of the application tied with the specified database.
    2. host = host name of the given database.
    3. user = user login credientals for the database.
    4. password = user login credientals for the database.
    5. database = the name of the database accessed within the server.
*/
module.exports = 
  devToolDB = {
    name: "devtoolDB",
    host: "localhost",
    user: "root",
    password: "plzpass4me",
    database: "bookmarktest"
  }
;
